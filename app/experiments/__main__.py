"""Allow running experiments as: python -m app.experiments.edge_sweep"""
# This file exists so the package is importable, but each experiment
# has its own __main__ block. Run experiments directly:
#   python -m app.experiments.edge_sweep --csv data/nba_calibration_ml.csv
